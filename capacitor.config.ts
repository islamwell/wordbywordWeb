import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ingenuitiveapps.khutbah',
  appName: 'Arabic Quranic Analysis',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
